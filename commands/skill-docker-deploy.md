---
description: 运行 /docker-deploy skill 创建 Docker 部署相关文件。包括 Dockerfile（使用 uv 多阶段构建）、.dockerignore 和 build.sh 构建脚本。用于容器化 Python FastAPI 项目并推送到火山引擎镜像仓库。
---

运行 /docker-deploy skill 创建 Docker 部署相关文件。包括 Dockerfile（使用 uv 多阶段构建）、.dockerignore 和 build.sh 构建脚本。用于容器化 Python FastAPI 项目并推送到火山引擎镜像仓库。
